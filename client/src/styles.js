import { makeStyles } from '@material-ui/core/styles';

// Add MUI component styling here by setting a
// className for on a div that encloses
// the object needing styling and adding the
// className object styling below
// 
// Example for on a page in client/src/pages
// <div className={classes.container}>
// 
// Example for styling list on this page
// container: {
//      backgroundColor: theme.palette.background.paper
// },

const useStyles = makeStyles((theme) => ({

}));

export default useStyles;